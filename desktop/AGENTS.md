# Working on Undertrained Indoor

The owner requests that UI design and implementation be delegated to **Claude Fable 5.1** through the installed Claude Code CLI and their Claude subscription.

- Use the explicit model ID `claude-fable-5-1`. Do not silently substitute a different model.
- Delegate UI changes, including layout, styling, interaction design and interface copy. The coordinating agent should review the result, verify behavior and run checks.
- Use the existing Claude Code subscription login. Do not extract credentials or switch to separately billed API credentials.
- Ask Claude to inspect rendered native screenshots and preserve the real authentication and BLE behavior.
- Keep fixture data confined to developer tests. Do not add demo mode or represent unimplemented workout functionality as available.
- If the requested model cannot be used, explain the blocker before doing UI implementation with another model.

Read `README.md` for build commands and the current scope. Authentication changes in the web backend belong in its separate repository.
